{
    "name": "ide-widgets-bootstrap",
    "version": "0.0.1-alpha.2",
    "displayName": "Bootstrap Widgets For BlockLang IDE",
    "repository": {
        "type": "git",
        "url": "https://github.com/blocklang/ide-widgets-bootstrap.git"
    },
    "scripts": {
        "dev": "dojo build --mode dev --watch --serve",
        "build": "dojo build --mode dist --single-bundle --omit-hash && shx cp src/icons.svg output/dist",
        "build:dev": "dojo build --mode dev",
        "build:widget": "dojo build widget --mode dist --target lib",
        "test": "dojo test",
        "test:unit": "dojo build --mode unit && dojo test --unit --config local",
        "test:functional": "dojo build --mode functional && dojo test --functional --config local",
        "test:all": "dojo build --mode unit && dojo build --mode functional && dojo test --all --config local",
        "precommit": "lint-staged",
        "prettier": "prettier --write 'src/**/*.{ts, tsx}' 'src/**/*.css' 'tests/**/*.{ts,tsx}'"
    },
    "dependencies": {
        "@dojo/framework": "^6.0.3",
        "@dojo/themes": "^6.0.0",
        "bootstrap-classes": "~0.0.2",
        "designer-core": "~0.0.1-alpha.21",
        "dojo-fontawesome": "~0.0.2",
        "tslib": "~1.9.1",
        "widgets-bootstrap": "~0.0.1-alpha.2"
    },
    "devDependencies": {
        "@dojo/cli": "^6.0.0",
        "@dojo/cli-build-app": "^6.0.9",
        "@dojo/cli-test-intern": "^6.0.0",
        "@types/node": "^9.6.53",
        "@types/sinon": "^7.0.13",
        "husky": "^3.0.9",
        "lint-staged": "^9.4.2",
        "prettier": "^1.18.2",
        "shx": "^0.3.2",
        "sinon": "^7.5.0",
        "typescript": "~3.4.5"
    },
    "lint-staged": {
        "*.{ts,tsx,css}": [
            "prettier --write",
            "git add"
        ]
    },
    "prettier": {
        "singleQuote": false,
        "tabWidth": 4,
        "useTabs": true,
        "printWidth": 120,
        "arrowParens": "always"
    }
}
